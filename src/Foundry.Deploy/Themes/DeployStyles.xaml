<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="SectionCardBorderStyle"
        TargetType="Border">
        <Setter
            Property="Padding"
            Value="16" />
        <Setter
            Property="Margin"
            Value="0,0,0,16" />
        <Setter
            Property="CornerRadius"
            Value="8" />
        <Setter
            Property="BorderThickness"
            Value="1" />
        <Setter
            Property="Background"
            Value="{DynamicResource CardBackgroundFillColorDefaultBrush}" />
        <Setter
            Property="BorderBrush"
            Value="{DynamicResource CardStrokeColorDefaultBrush}" />
    </Style>

    <Style x:Key="WizardStepButtonStyle"
        TargetType="Button">
        <Setter
            Property="HorizontalContentAlignment"
            Value="Stretch" />
        <Setter
            Property="HorizontalAlignment"
            Value="Stretch" />
        <Setter
            Property="Padding"
            Value="12,10" />
        <Setter
            Property="Margin"
            Value="0,0,0,6" />
        <Setter
            Property="Background"
            Value="Transparent" />
        <Setter
            Property="BorderBrush"
            Value="Transparent" />
        <Setter
            Property="BorderThickness"
            Value="1" />
        <Setter
            Property="IsTabStop"
            Value="True" />
        <Style.Triggers>
            <DataTrigger
                Binding="{Binding IsCurrent}"
                Value="True">
                <Setter
                    Property="Background"
                    Value="{DynamicResource SubtleFillColorSecondaryBrush}" />
                <Setter
                    Property="BorderBrush"
                    Value="{DynamicResource AccentFillColorDefaultBrush}" />
            </DataTrigger>
            <DataTrigger
                Binding="{Binding IsFuture}"
                Value="True">
                <Setter
                    Property="Opacity"
                    Value="0.62" />
                <Setter
                    Property="IsTabStop"
                    Value="False" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="WizardContentCardStyle"
        BasedOn="{StaticResource SectionCardBorderStyle}"
        TargetType="Border">
        <Setter
            Property="MaxWidth"
            Value="980" />
        <Setter
            Property="HorizontalAlignment"
            Value="Stretch" />
        <Setter
            Property="Margin"
            Value="0" />
    </Style>

    <Style x:Key="DeployProgressBarStyle"
        BasedOn="{StaticResource {x:Type ProgressBar}}"
        TargetType="ProgressBar">
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect
                    BlurRadius="20"
                    Opacity="1"
                    ShadowDepth="0"
                    Color="#00B7FF" />
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
