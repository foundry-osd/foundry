<UserControl x:Class="Foundry.Deploy.Views.SplashView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <StackPanel
            MaxWidth="760"
            Margin="48,32"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <StackPanel
                Margin="0,0,0,16"
                HorizontalAlignment="Center"
                Orientation="Horizontal">
                <StackPanel.RenderTransform>
                    <TranslateTransform X="-8" />
                </StackPanel.RenderTransform>
                <Image
                    Width="96"
                    Height="96"
                    Margin="0,0,16,0"
                    VerticalAlignment="Center"
                    AutomationProperties.Name="{Binding Strings[App.Name]}"
                    Source="/Assets/Images/foundry-about-logo.png"
                    Stretch="Uniform" />
                <TextBlock
                    VerticalAlignment="Center"
                    Style="{StaticResource TitleLargeTextBlockStyle}"
                    Text="{Binding Strings[App.Name]}" />
            </StackPanel>
            <TextBlock
                HorizontalAlignment="Center"
                Style="{StaticResource DisplayTextBlockStyle}"
                Text="{Binding Strings[Splash.Welcome]}"
                TextAlignment="Center"
                TextWrapping="Wrap" />

            <Grid x:Name="LandingActionHost"
                MinHeight="76"
                Margin="0,12,0,0"
                RenderTransformOrigin="0.5,0.5">
                <Grid.RenderTransform>
                    <TranslateTransform x:Name="LandingActionTransform" />
                </Grid.RenderTransform>
                <StackPanel
                    MinWidth="260"
                    MaxWidth="520"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Visibility="{Binding Session.IsStartupInitializing, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ProgressBar
                        Height="4"
                        IsIndeterminate="True"
                        Style="{StaticResource DeployProgressBarStyle}" />
                    <TextBlock
                        Margin="0,12,0,0"
                        HorizontalAlignment="Center"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Style="{StaticResource BodyTextBlockStyle}"
                        Text="{Binding Strings[Splash.InitializingComponents]}"
                        TextAlignment="Center" />
                </StackPanel>

                <Button
                    Height="38"
                    MinWidth="210"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    AutomationProperties.Name="{Binding Strings[Splash.StartDeployment]}"
                    Command="{Binding BeginWizardCommand}"
                    Content="{Binding Strings[Splash.StartDeployment]}"
                    Style="{DynamicResource AccentButtonStyle}"
                    Visibility="{Binding Session.IsStartupInitializing, Converter={StaticResource InverseBooleanConverter}}" />
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
