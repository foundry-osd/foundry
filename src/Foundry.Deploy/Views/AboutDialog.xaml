<Window
    x:Class="Foundry.Deploy.Views.AboutDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="About Foundry.Deploy"
    MinWidth="640"
    MinHeight="380"
    Icon="/Assets/Icons/app.ico"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    SizeToContent="WidthAndHeight"
    WindowStartupLocation="CenterOwner">
    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding CloseCommand}" />
    </Window.InputBindings>

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Image
                Grid.Column="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Source="/Assets/Images/foundry-deploy-about-logo.png"
                Stretch="None" />

            <StackPanel
                Grid.Column="1"
                Margin="24,0,0,0"
                VerticalAlignment="Center">
                <TextBlock
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Style="{DynamicResource TitleLargeTextBlockStyle}"
                    Text="{Binding AppName}" />

                <TextBlock
                    Margin="0,12,0,0"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Style="{DynamicResource SubtitleTextBlockStyle}"
                    Text="{Binding Version}" />

                <TextBlock
                    Margin="0,24,0,0"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Style="{DynamicResource BodyTextBlockStyle}"
                    Text="{Binding DescriptionLine1}"
                    TextWrapping="Wrap" />

                <TextBlock
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Style="{DynamicResource BodyTextBlockStyle}"
                    Text="{Binding DescriptionLine2}"
                    TextWrapping="Wrap" />
            </StackPanel>
        </Grid>

        <TextBlock
            Grid.Row="1"
            HorizontalAlignment="Center"
            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
            Style="{StaticResource CaptionTextBlockStyle}"
            Text="{Binding Footer}"
            TextWrapping="Wrap" />
    </Grid>
</Window>
