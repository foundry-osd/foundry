<Window x:Class="Foundry.Deploy.Views.DeploymentErrorDetailsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="{Binding Strings[Error.TechnicalDetailsTitle]}"
    Width="760"
    Height="500"
    MinWidth="520"
    MinHeight="360"
    WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock
            Style="{StaticResource TitleTextBlockStyle}"
            Text="{Binding Strings[Error.TechnicalDetailsTitle]}" />
        <TextBox x:Name="DetailsTextBox"
            Grid.Row="1"
            Margin="0,16,0,16"
            AcceptsReturn="True"
            AcceptsTab="True"
            AutomationProperties.Name="{Binding Strings[Error.TechnicalDetailsTitle]}"
            FontFamily="Consolas"
            HorizontalScrollBarVisibility="Auto"
            IsReadOnly="True"
            Text="{Binding FailedStepErrorMessage}"
            TextWrapping="NoWrap"
            VerticalScrollBarVisibility="Auto" />
        <StackPanel
            Grid.Row="2"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                MinWidth="100"
                Margin="0,0,8,0"
                Click="Copy_Click"
                Content="{Binding Strings[Common.Copy]}" />
            <Button
                MinWidth="120"
                Margin="0,0,8,0"
                Command="{Binding OpenLogFileCommand}"
                Content="{Binding Strings[Tools.OpenLogFile]}" />
            <Button
                MinWidth="100"
                Content="{Binding Strings[Common.Close]}"
                IsCancel="True"
                IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
