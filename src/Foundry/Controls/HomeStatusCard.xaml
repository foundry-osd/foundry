<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="Foundry.Controls.HomeStatusCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    MinWidth="240"
    mc:Ignorable="d">

    <Grid
        Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
        CornerRadius="8">
        <Grid.Resources>
            <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <ResourceDictionary Source="HomeCardButtonResources.xaml" />
                </ResourceDictionary.MergedDictionaries>
            </ResourceDictionary>
        </Grid.Resources>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="StatusStates">
                <VisualState x:Name="Neutral" />
                <VisualState x:Name="Ready">
                    <VisualState.Setters>
                        <Setter
                            Target="StatusBadge.Background"
                            Value="{ThemeResource FoundryStatusReadyBrush}" />
                        <Setter
                            Target="StatusIcon.Foreground"
                            Value="{ThemeResource FoundryStatusReadyForegroundBrush}" />
                        <Setter
                            Target="StatusLabel.Foreground"
                            Value="{ThemeResource FoundryStatusReadyForegroundBrush}" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Blocked">
                    <VisualState.Setters>
                        <Setter
                            Target="StatusBadge.Background"
                            Value="{ThemeResource FoundryStatusBlockedBrush}" />
                        <Setter
                            Target="StatusIcon.Foreground"
                            Value="{ThemeResource FoundryStatusBlockedForegroundBrush}" />
                        <Setter
                            Target="StatusLabel.Foreground"
                            Value="{ThemeResource FoundryStatusBlockedForegroundBrush}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Button
            Padding="-1"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            HorizontalContentAlignment="Stretch"
            VerticalContentAlignment="Stretch"
            AutomationProperties.Name="{x:Bind AutomationName, Mode=OneTime}"
            Click="CardButton_Click"
            CornerRadius="{StaticResource OverlayCornerRadius}">
            <StackPanel
                Padding="20,16"
                Spacing="8">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <FontIcon
                        Grid.Column="0"
                        Margin="0,0,12,0"
                        VerticalAlignment="Center"
                        FontSize="24"
                        Glyph="{x:Bind IconGlyph, Mode=OneWay}" />
                    <TextBlock
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Style="{StaticResource BodyStrongTextBlockStyle}"
                        Text="{x:Bind Title, Mode=OneWay}" />
                    <Border x:Name="StatusBadge"
                        Grid.Column="2"
                        Padding="8,4"
                        Background="{ThemeResource FoundryStatusNeutralBrush}"
                        CornerRadius="12">
                        <StackPanel
                            Orientation="Horizontal"
                            Spacing="4">
                            <FontIcon x:Name="StatusIcon"
                                FontSize="10"
                                Foreground="{ThemeResource FoundryStatusNeutralForegroundBrush}"
                                Glyph="{x:Bind BadgeGlyph, Mode=OneWay}" />
                            <TextBlock x:Name="StatusLabel"
                                VerticalAlignment="Center"
                                Foreground="{ThemeResource FoundryStatusNeutralForegroundBrush}"
                                Style="{ThemeResource FoundryCaptionTextBlockStyle}"
                                Text="{x:Bind StatusText, Mode=OneWay}" />
                        </StackPanel>
                    </Border>
                </Grid>

                <Rectangle
                    Height="1"
                    Fill="{ThemeResource DividerStrokeColorDefaultBrush}"
                    Opacity="0.35" />

                <Grid
                    ColumnSpacing="12"
                    RowSpacing="6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="0"
                        Style="{ThemeResource FoundryCaptionTextBlockStyle}"
                        Text="{x:Bind Line1Label, Mode=OneWay}"
                        TextWrapping="Wrap"
                        Visibility="{x:Bind Line1Visibility, Mode=OneWay}" />
                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="1"
                        Text="{x:Bind Line1Value, Mode=OneWay}"
                        TextWrapping="Wrap"
                        Visibility="{x:Bind Line1Visibility, Mode=OneWay}" />

                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="0"
                        Style="{ThemeResource FoundryCaptionTextBlockStyle}"
                        Text="{x:Bind Line2Label, Mode=OneWay}"
                        TextWrapping="Wrap"
                        Visibility="{x:Bind Line2Visibility, Mode=OneWay}" />
                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="1"
                        Text="{x:Bind Line2Value, Mode=OneWay}"
                        TextWrapping="Wrap"
                        Visibility="{x:Bind Line2Visibility, Mode=OneWay}" />

                    <TextBlock
                        Grid.Row="2"
                        Grid.Column="0"
                        Style="{ThemeResource FoundryCaptionTextBlockStyle}"
                        Text="{x:Bind Line3Label, Mode=OneWay}"
                        TextWrapping="Wrap"
                        Visibility="{x:Bind Line3Visibility, Mode=OneWay}" />
                    <TextBlock
                        Grid.Row="2"
                        Grid.Column="1"
                        Text="{x:Bind Line3Value, Mode=OneWay}"
                        TextWrapping="Wrap"
                        Visibility="{x:Bind Line3Visibility, Mode=OneWay}" />

                    <TextBlock
                        Grid.Row="3"
                        Grid.Column="0"
                        Style="{ThemeResource FoundryCaptionTextBlockStyle}"
                        Text="{x:Bind Line4Label, Mode=OneWay}"
                        TextWrapping="Wrap"
                        Visibility="{x:Bind Line4Visibility, Mode=OneWay}" />
                    <TextBlock
                        Grid.Row="3"
                        Grid.Column="1"
                        Text="{x:Bind Line4Value, Mode=OneWay}"
                        TextWrapping="Wrap"
                        Visibility="{x:Bind Line4Visibility, Mode=OneWay}" />
                </Grid>
            </StackPanel>
        </Button>
    </Grid>
</UserControl>
