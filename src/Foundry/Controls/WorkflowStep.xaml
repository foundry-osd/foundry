<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="Foundry.Controls.WorkflowStep"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="80"
    mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Light">
                    <SolidColorBrush x:Key="WorkflowStepReadyBrush"
                        Color="{ThemeResource SystemFillColorSuccess}" />
                    <SolidColorBrush x:Key="WorkflowStepCriticalBrush"
                        Color="{ThemeResource SystemFillColorCritical}" />
                    <SolidColorBrush x:Key="WorkflowStepPendingBrush"
                        Color="{ThemeResource ControlFillColorSecondary}" />
                    <SolidColorBrush x:Key="WorkflowStepReadyForegroundBrush"
                        Color="White" />
                    <StaticResource x:Key="WorkflowStepCriticalForegroundBrush"
                        ResourceKey="TextFillColorInverseBrush" />
                    <SolidColorBrush x:Key="WorkflowStepPendingForegroundBrush"
                        Color="{ThemeResource TextFillColorSecondary}" />
                </ResourceDictionary>
                <ResourceDictionary x:Key="Dark">
                    <SolidColorBrush x:Key="WorkflowStepReadyBrush"
                        Color="{ThemeResource SystemFillColorSuccess}" />
                    <SolidColorBrush x:Key="WorkflowStepCriticalBrush"
                        Color="{ThemeResource SystemFillColorCritical}" />
                    <SolidColorBrush x:Key="WorkflowStepPendingBrush"
                        Color="{ThemeResource ControlFillColorSecondary}" />
                    <SolidColorBrush x:Key="WorkflowStepReadyForegroundBrush"
                        Color="White" />
                    <StaticResource x:Key="WorkflowStepCriticalForegroundBrush"
                        ResourceKey="TextFillColorInverseBrush" />
                    <SolidColorBrush x:Key="WorkflowStepPendingForegroundBrush"
                        Color="{ThemeResource TextFillColorSecondary}" />
                </ResourceDictionary>
                <ResourceDictionary x:Key="HighContrast">
                    <SolidColorBrush x:Key="WorkflowStepReadyBrush"
                        Color="{ThemeResource SystemColorButtonFaceColor}" />
                    <SolidColorBrush x:Key="WorkflowStepCriticalBrush"
                        Color="{ThemeResource SystemColorButtonFaceColor}" />
                    <SolidColorBrush x:Key="WorkflowStepPendingBrush"
                        Color="{ThemeResource SystemColorButtonFaceColor}" />
                    <SolidColorBrush x:Key="WorkflowStepReadyForegroundBrush"
                        Color="{ThemeResource SystemColorButtonTextColor}" />
                    <SolidColorBrush x:Key="WorkflowStepCriticalForegroundBrush"
                        Color="{ThemeResource SystemColorButtonTextColor}" />
                    <SolidColorBrush x:Key="WorkflowStepPendingForegroundBrush"
                        Color="{ThemeResource SystemColorButtonTextColor}" />
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="StatusStates">
                <VisualState x:Name="Pending" />
                <VisualState x:Name="Ready">
                    <VisualState.Setters>
                        <Setter
                            Target="StepCircle.Background"
                            Value="{ThemeResource WorkflowStepReadyBrush}" />
                        <Setter
                            Target="StepLabel.Foreground"
                            Value="{ThemeResource TextFillColorPrimaryBrush}" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Critical">
                    <VisualState.Setters>
                        <Setter
                            Target="StepCircle.Background"
                            Value="{ThemeResource WorkflowStepCriticalBrush}" />
                        <Setter
                            Target="StepLabel.Foreground"
                            Value="{ThemeResource TextFillColorPrimaryBrush}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Button
            Padding="4"
            HorizontalAlignment="Stretch"
            HorizontalContentAlignment="Center"
            AutomationProperties.Name="{x:Bind AutomationName, Mode=OneTime}"
            Background="Transparent"
            BorderThickness="0"
            Click="StepButton_Click">
            <StackPanel
                HorizontalAlignment="Center"
                Orientation="Vertical"
                Spacing="8">
                <Grid x:Name="StepCircle"
                    Width="40"
                    Height="40"
                    HorizontalAlignment="Center"
                    Background="{ThemeResource WorkflowStepPendingBrush}"
                    CornerRadius="20">
                    <FontIcon
                        FontSize="16"
                        Foreground="{ThemeResource WorkflowStepReadyForegroundBrush}"
                        Glyph="&#xE73E;"
                        Visibility="{x:Bind IsReady, Mode=OneTime}" />
                    <FontIcon
                        FontSize="16"
                        Foreground="{ThemeResource WorkflowStepCriticalForegroundBrush}"
                        Glyph="&#xE711;"
                        Visibility="{x:Bind IsCritical, Mode=OneTime}" />
                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Foreground="{ThemeResource WorkflowStepPendingForegroundBrush}"
                        Text="{x:Bind StepNumber, Mode=OneWay}"
                        Visibility="{x:Bind IsPending, Mode=OneTime}" />
                </Grid>
                <TextBlock x:Name="StepLabel"
                    HorizontalAlignment="Center"
                    FontSize="12"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="{x:Bind Label, Mode=OneWay}"
                    TextAlignment="Center"
                    TextWrapping="Wrap" />
            </StackPanel>
        </Button>
    </Grid>
</UserControl>
