<UserControl
    x:Class="Foundry.Views.NetworkSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock
            Margin="0,0,0,8"
            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
            Style="{StaticResource TitleTextBlockStyle}"
            Text="{Binding Strings[ExpertSectionNetwork]}" />
        <Separator Margin="0,0,0,12" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />

        <CheckBox
            Margin="0,0,0,12"
            Content="{Binding Strings[Dot1xEnableLabel]}"
            IsChecked="{Binding IsDot1xEnabled}" />

        <Grid Margin="0,0,0,24" IsEnabled="{Binding IsDot1xEnabled}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Margin="0,0,8,0"
                VerticalAlignment="Center"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="{Binding Strings[Dot1xCertificateLabel]}" />
            <TextBox
                Grid.Column="1"
                Margin="0,0,8,0"
                Text="{Binding CertificatePath, UpdateSourceTrigger=PropertyChanged}" />
            <Button
                Grid.Column="2"
                Command="{Binding BrowseCertificateCommand}"
                Content="{Binding Strings[BrowseFileButton]}" />
        </Grid>

        <CheckBox
            Margin="0,0,0,12"
            Content="{Binding Strings[WifiEnableLabel]}"
            IsChecked="{Binding IsWifiEnabled}" />

        <Grid IsEnabled="{Binding IsWifiEnabled}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                Grid.Column="0"
                Margin="0,0,8,8"
                VerticalAlignment="Center"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="{Binding Strings[WifiSsidLabel]}" />
            <TextBox
                Grid.Row="0"
                Grid.Column="1"
                Margin="0,0,0,8"
                Text="{Binding WifiSsid, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock
                Grid.Row="1"
                Grid.Column="0"
                Margin="0,0,8,0"
                VerticalAlignment="Center"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="{Binding Strings[WifiSecurityTypeLabel]}" />
            <ComboBox
                Grid.Row="1"
                Grid.Column="1"
                ItemsSource="{Binding AvailableSecurityTypes}"
                SelectedItem="{Binding WifiSecurityType}" />
        </Grid>
    </StackPanel>
</UserControl>
