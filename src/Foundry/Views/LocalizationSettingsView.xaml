<UserControl
    x:Class="Foundry.Views.LocalizationSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock
            Margin="0,0,0,8"
            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
            Style="{StaticResource TitleTextBlockStyle}"
            Text="{Binding Strings[Expert.SectionLocalization]}" />
        <Separator Margin="0,0,0,12" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />

        <TextBlock
            Margin="0,0,0,8"
            Style="{StaticResource BodyStrongTextBlockStyle}"
            Text="{Binding Strings[Localization.VisibleLanguagesLabel]}" />

        <Border
            Padding="12"
            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8">
            <ItemsControl ItemsSource="{Binding AvailableLanguages}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding DisplayName}" IsChecked="{Binding IsSelected}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <Grid Margin="0,16,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Margin="0,0,8,0"
                VerticalAlignment="Center"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="{Binding Strings[Localization.DefaultOverrideLabel]}" />
            <ComboBox
                Grid.Column="1"
                DisplayMemberPath="DisplayName"
                ItemsSource="{Binding VisibleLanguages}"
                SelectedValue="{Binding SelectedDefaultLanguageCode}"
                SelectedValuePath="Code" />
        </Grid>

        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Margin="0,0,8,0"
                VerticalAlignment="Center"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="{Binding Strings[Localization.TimeZoneLabel]}" />
            <ComboBox
                Grid.Column="1"
                DisplayMemberPath="DisplayName"
                ItemsSource="{Binding AvailableTimeZones}"
                SelectedValue="{Binding SelectedTimeZoneId}"
                SelectedValuePath="Id" />
        </Grid>

        <CheckBox
            Content="{Binding Strings[Localization.ForceSingleLabel]}"
            IsChecked="{Binding ForceSingleVisibleLanguage}" />
    </StackPanel>
</UserControl>
