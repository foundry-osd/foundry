<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="Foundry.Views.DeploymentProfilesControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="using:Foundry.Behaviors"
    xmlns:wct="using:CommunityToolkit.WinUI.Controls">
    <UserControl.Resources>
        <x:Double x:Key="SettingsCardWrapThreshold">1000</x:Double>
        <Style x:Key="ProfileActionButtonStyle"
            BasedOn="{StaticResource DefaultButtonStyle}"
            TargetType="Button">
            <Setter
                Property="MinWidth"
                Value="{ThemeResource FoundryCompactActionMinWidth}" />
            <Setter
                Property="HorizontalAlignment"
                Value="Stretch" />
            <Setter
                Property="VerticalAlignment"
                Value="Stretch" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="CompactProfileActions">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter
                            Target="ProfileSelector.Width"
                            Value="{ThemeResource FoundrySettingsActionMinWidth}" />
                        <Setter
                            Target="ProfileActions.Width"
                            Value="{ThemeResource FoundryWideInputMinWidth}" />
                        <Setter
                            Target="ProfileActions.RowSpacing"
                            Value="{ThemeResource FoundryInlineControlSpacing}" />
                        <Setter
                            Target="SaveCopyButton.(Grid.ColumnSpan)"
                            Value="2" />
                        <Setter
                            Target="ImportButton.(Grid.Column)"
                            Value="2" />
                        <Setter
                            Target="ImportButton.(Grid.ColumnSpan)"
                            Value="2" />
                        <Setter
                            Target="ExportButton.(Grid.Row)"
                            Value="1" />
                        <Setter
                            Target="ExportButton.(Grid.Column)"
                            Value="0" />
                        <Setter
                            Target="ExportButton.(Grid.ColumnSpan)"
                            Value="2" />
                        <Setter
                            Target="ManageButton.(Grid.Row)"
                            Value="1" />
                        <Setter
                            Target="ManageButton.(Grid.Column)"
                            Value="2" />
                        <Setter
                            Target="ManageButton.(Grid.ColumnSpan)"
                            Value="2" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="WideProfileActions">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1500" />
                    </VisualState.StateTriggers>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <StackPanel Spacing="{ThemeResource FoundryCompactStackSpacing}">
            <wct:SettingsExpander
                Description="{x:Bind ViewModel.Text('SettingsPage_ProfilesCard.Description')}"
                Header="{x:Bind ViewModel.Text('SettingsPage_ProfilesCard.Header')}">
                <wct:SettingsExpander.HeaderIcon>
                    <FontIcon Glyph="&#xE8A5;" />
                </wct:SettingsExpander.HeaderIcon>
                <Grid
                    Margin="{ThemeResource FoundrySettingsExpanderTrailingAffordanceMargin}"
                    ColumnSpacing="{ThemeResource FoundryInlineControlSpacing}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <ComboBox x:Name="ProfileSelector"
                        Width="{ThemeResource FoundryWideInputMinWidth}"
                        VerticalAlignment="Center"
                        AutomationProperties.Name="{x:Bind ViewModel.Text('Profiles.Name')}"
                        IsEnabled="{x:Bind ViewModel.CanInteract, Mode=OneWay}"
                        ItemsSource="{x:Bind ViewModel.Profiles}"
                        SelectedItem="{x:Bind ViewModel.SelectedProfile, Mode=TwoWay}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock
                                    Text="{Binding DisplayName}"
                                    TextTrimming="CharacterEllipsis"
                                    ToolTipService.ToolTip="{Binding DisplayName}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <Button
                        Grid.Column="1"
                        MinWidth="{ThemeResource FoundryCompactActionMinWidth}"
                        VerticalAlignment="Center"
                        Command="{x:Bind ViewModel.ActivateCommand}"
                        Content="{x:Bind ViewModel.Text('Profiles.Activate')}"
                        IsEnabled="{x:Bind ViewModel.CanActivate, Mode=OneWay}" />
                </Grid>
                <wct:SettingsExpander.Items>
                    <wct:SettingsCard
                        Description="{x:Bind ViewModel.ActiveProfileName, Mode=OneWay}"
                        Header="{x:Bind ViewModel.Text('Profiles.Active')}"
                        IsEnabled="{x:Bind ViewModel.CanInteract, Mode=OneWay}">
                        <Grid x:Name="ProfileActions"
                            Width="{ThemeResource FoundrySettingsControlMaxWidth}"
                            ColumnSpacing="{ThemeResource FoundryInlineControlSpacing}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Button x:Name="SaveCopyButton"
                                Command="{x:Bind ViewModel.SaveCopyCommand}"
                                Style="{StaticResource ProfileActionButtonStyle}">
                                <TextBlock
                                    Text="{x:Bind ViewModel.Text('Profiles.SaveCopy')}"
                                    TextAlignment="Center"
                                    TextWrapping="Wrap" />
                            </Button>
                            <Button x:Name="ImportButton"
                                Grid.Column="1"
                                Command="{x:Bind ViewModel.ImportCommand}"
                                Style="{StaticResource ProfileActionButtonStyle}">
                                <TextBlock
                                    Text="{x:Bind ViewModel.Text('Profiles.Import')}"
                                    TextAlignment="Center"
                                    TextWrapping="Wrap" />
                            </Button>
                            <Button x:Name="ExportButton"
                                Grid.Column="2"
                                Command="{x:Bind ViewModel.ExportCommand}"
                                IsEnabled="{x:Bind ViewModel.HasActive, Mode=OneWay}"
                                Style="{StaticResource ProfileActionButtonStyle}">
                                <TextBlock
                                    Text="{x:Bind ViewModel.Text('Profiles.Export')}"
                                    TextAlignment="Center"
                                    TextWrapping="Wrap" />
                            </Button>
                            <DropDownButton x:Name="ManageButton"
                                Grid.Column="3"
                                MinWidth="{ThemeResource FoundryCompactActionMinWidth}"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                Content="{x:Bind ViewModel.Text('Profiles.Manage')}">
                                <DropDownButton.Flyout>
                                    <MenuFlyout>
                                        <MenuFlyoutItem
                                            Command="{x:Bind ViewModel.RenameCommand}"
                                            IsEnabled="{x:Bind ViewModel.HasActive, Mode=OneWay}"
                                            Text="{x:Bind ViewModel.Text('Profiles.Rename')}" />
                                        <MenuFlyoutSeparator />
                                        <MenuFlyoutItem
                                            Command="{x:Bind ViewModel.CreateSharedCommand}"
                                            IsEnabled="{x:Bind ViewModel.HasActive, Mode=OneWay}"
                                            Text="{x:Bind ViewModel.Text('Profiles.CreateShared')}" />
                                        <MenuFlyoutItem
                                            Command="{x:Bind ViewModel.JoinSharedCommand}"
                                            Text="{x:Bind ViewModel.Text('Profiles.JoinShared')}" />
                                        <MenuFlyoutItem
                                            Command="{x:Bind ViewModel.ExportRecoveryCommand}"
                                            IsEnabled="{x:Bind ViewModel.IsShared, Mode=OneWay}"
                                            Text="{x:Bind ViewModel.Text('Profiles.ExportRecovery')}" />
                                        <MenuFlyoutSeparator />
                                        <MenuFlyoutItem
                                            Command="{x:Bind ViewModel.ForgetCommand}"
                                            IsEnabled="{x:Bind ViewModel.HasActive, Mode=OneWay}"
                                            Text="{x:Bind ViewModel.Text('Profiles.Forget')}" />
                                        <MenuFlyoutItem
                                            Command="{x:Bind ViewModel.DeleteCommand}"
                                            IsEnabled="{x:Bind ViewModel.HasActive, Mode=OneWay}"
                                            Text="{x:Bind ViewModel.Text('Profiles.Delete')}" />
                                    </MenuFlyout>
                                </DropDownButton.Flyout>
                            </DropDownButton>
                        </Grid>
                    </wct:SettingsCard>
                    <wct:SettingsCard
                        Description="{x:Bind ViewModel.Text('Profiles.LocalPrivacy')}"
                        Header="{x:Bind ViewModel.Text('Profiles.Remember')}"
                        IsEnabled="{x:Bind ViewModel.CanInteract, Mode=OneWay}">
                        <StackPanel
                            Orientation="Horizontal"
                            Spacing="{ThemeResource FoundryInlineControlSpacing}">
                            <TextBlock
                                VerticalAlignment="Center"
                                Text="{x:Bind ViewModel.RememberState, Mode=OneWay}" />
                            <Button
                                MinWidth="{ThemeResource FoundryCompactActionMinWidth}"
                                Command="{x:Bind ViewModel.RememberCommand}"
                                Content="{x:Bind ViewModel.Text('Profiles.Manage')}"
                                IsEnabled="{x:Bind ViewModel.HasActive, Mode=OneWay}" />
                        </StackPanel>
                    </wct:SettingsCard>
                    <wct:SettingsCard
                        Header="{x:Bind ViewModel.Text('Profiles.AutoSync')}"
                        IsEnabled="{x:Bind ViewModel.CanInteract, Mode=OneWay}"
                        Visibility="{x:Bind ViewModel.SharedVisibility, Mode=OneWay}">
                        <StackPanel
                            Orientation="Horizontal"
                            Spacing="{ThemeResource FoundryInlineControlSpacing}">
                            <Button
                                MinWidth="{ThemeResource FoundryCompactActionMinWidth}"
                                VerticalAlignment="Center"
                                Command="{x:Bind ViewModel.SynchronizeCommand}"
                                Content="{x:Bind ViewModel.Text('Profiles.SyncNow')}" />
                            <ToggleSwitch
                                behaviors:LocalizedToggleSwitch.UseLocalizedStateContent="True"
                                AutomationProperties.Name="{x:Bind ViewModel.Text('Profiles.AutoSync')}"
                                IsOn="{x:Bind ViewModel.SyncEnabled, Mode=OneWay}"
                                Toggled="AutomaticSyncToggle_Toggled" />
                        </StackPanel>
                    </wct:SettingsCard>
                </wct:SettingsExpander.Items>
            </wct:SettingsExpander>
            <InfoBar
                Title="{x:Bind ViewModel.Text('Profiles.Conflict')}"
                IsClosable="False"
                IsOpen="True"
                Message="{x:Bind ViewModel.Text('Profiles.ConflictWarning')}"
                Severity="Warning"
                Visibility="{x:Bind ViewModel.ConflictVisibility, Mode=OneWay}">
                <ContentControl IsEnabled="{x:Bind ViewModel.CanInteract, Mode=OneWay}">
                    <StackPanel Spacing="{ThemeResource FoundryInlineControlSpacing}">
                        <Button
                            MinWidth="{ThemeResource FoundryCompactActionMinWidth}"
                            Command="{x:Bind ViewModel.UseRemoteCommand}"
                            Content="{x:Bind ViewModel.Text('Profiles.UseRemote')}" />
                        <Button
                            MinWidth="{ThemeResource FoundryCompactActionMinWidth}"
                            Command="{x:Bind ViewModel.KeepLocalCommand}"
                            Content="{x:Bind ViewModel.Text('Profiles.KeepLocal')}" />
                        <Button
                            MinWidth="{ThemeResource FoundryCompactActionMinWidth}"
                            Command="{x:Bind ViewModel.SaveCopyCommand}"
                            Content="{x:Bind ViewModel.Text('Profiles.SaveCopy')}" />
                    </StackPanel>
                </ContentControl>
            </InfoBar>
            <TextBlock
                Margin="{ThemeResource FoundryDefaultContentPadding}"
                AutomationProperties.LiveSetting="Polite"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="{x:Bind ViewModel.Status, Mode=OneWay}"
                TextWrapping="Wrap"
                Visibility="{x:Bind ViewModel.StatusVisibility, Mode=OneWay}" />
            <ProgressBar
                IsIndeterminate="{x:Bind ViewModel.IsBusy, Mode=OneWay}"
                Visibility="{x:Bind ViewModel.BusyVisibility, Mode=OneWay}" />
        </StackPanel>
    </Grid>
</UserControl>
