<Window
    x:Class="Foundry.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Foundry"
    Width="600"
    Height="800"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="_File">
                <MenuItem Command="{Binding ExitCommand}" Header="E_xit" />
            </MenuItem>
            <MenuItem Header="_Options">
                <MenuItem
                    Header="_Advanced"
                    IsCheckable="True"
                    IsChecked="{Binding IsAdvancedEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </MenuItem>
            <MenuItem Header="_Theme">
                <MenuItem Command="{Binding SetSystemThemeCommand}" Header="_System" />
                <MenuItem Command="{Binding SetLightThemeCommand}" Header="_Light" />
                <MenuItem Command="{Binding SetDarkThemeCommand}" Header="_Dark" />
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Command="{Binding ShowAboutCommand}" Header="_About" />
            </MenuItem>
        </Menu>

        <Frame
            x:Name="ContentFrame"
            Grid.Row="1"
            NavigationUIVisibility="Hidden" />
    </Grid>
</Window>
