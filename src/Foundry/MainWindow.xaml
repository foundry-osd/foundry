<Window
    x:Class="Foundry.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:Foundry.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Foundry"
    Width="860"
    SizeToContent="Height"
    ResizeMode="CanMinimize"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Window.Resources>
        <converters:CultureToBooleanConverter x:Key="CultureToBooleanConverter" />
        <converters:ThemeModeToBooleanConverter x:Key="ThemeModeToBooleanConverter" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
            Background="Transparent"
            BorderBrush="{DynamicResource TextFillColorPrimaryBrush}"
            BorderThickness="0,0,0,1">
            <Menu>
                <MenuItem Header="{Binding Strings[MenuFile]}">
                    <MenuItem Command="{Binding ExitCommand}" Header="{Binding Strings[MenuExit]}" />
                </MenuItem>
                <MenuItem Header="{Binding Strings[MenuTheme]}">
                    <MenuItem
                        Command="{Binding SetSystemThemeCommand}"
                        Header="{Binding Strings[ThemeSystem]}"
                        IsCheckable="True"
                        IsChecked="{Binding CurrentTheme, Converter={StaticResource ThemeModeToBooleanConverter}, ConverterParameter=System, Mode=OneWay}" />
                    <MenuItem
                        Command="{Binding SetLightThemeCommand}"
                        Header="{Binding Strings[ThemeLight]}"
                        IsCheckable="True"
                        IsChecked="{Binding CurrentTheme, Converter={StaticResource ThemeModeToBooleanConverter}, ConverterParameter=Light, Mode=OneWay}" />
                    <MenuItem
                        Command="{Binding SetDarkThemeCommand}"
                        Header="{Binding Strings[ThemeDark]}"
                        IsCheckable="True"
                        IsChecked="{Binding CurrentTheme, Converter={StaticResource ThemeModeToBooleanConverter}, ConverterParameter=Dark, Mode=OneWay}" />
                </MenuItem>
                <MenuItem Header="{Binding Strings[MenuLanguage]}">
                    <MenuItem
                        Command="{Binding SetCultureCommand}"
                        CommandParameter="en-US"
                        Header="{Binding Strings[LanguageEnglish]}"
                        IsCheckable="True"
                        IsChecked="{Binding CurrentCulture, Converter={StaticResource CultureToBooleanConverter}, ConverterParameter=en-US, Mode=OneWay}" />
                    <MenuItem
                        Command="{Binding SetCultureCommand}"
                        CommandParameter="fr-FR"
                        Header="{Binding Strings[LanguageFrench]}"
                        IsCheckable="True"
                        IsChecked="{Binding CurrentCulture, Converter={StaticResource CultureToBooleanConverter}, ConverterParameter=fr-FR, Mode=OneWay}" />
                </MenuItem>
                <MenuItem Header="{Binding Strings[MenuHelp]}">
                    <MenuItem Command="{Binding ShowAboutCommand}" Header="{Binding Strings[MenuAbout]}" />
                </MenuItem>
            </Menu>
        </Border>

        <!--  ADK Banner  -->
        <Border
            Grid.Row="1"
            Padding="16,12"
            Background="#D97706"
            BorderBrush="#B45309"
            BorderThickness="0,2,0,2"
            Visibility="{Binding ShowAdkBanner, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Style="{StaticResource BodyStrongTextBlockStyle}"
                    Text="{Binding Strings[AdkBannerMissing]}"
                    TextWrapping="Wrap"
                    Visibility="{Binding IsAdkMissing, Converter={StaticResource BooleanToVisibilityConverter}}" />

                <TextBlock
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Style="{StaticResource BodyStrongTextBlockStyle}"
                    Text="{Binding Strings[AdkBannerIncompatible]}"
                    TextWrapping="Wrap"
                    Visibility="{Binding IsAdkIncompatible, Converter={StaticResource BooleanToVisibilityConverter}}" />

                <Button
                    Grid.Column="1"
                    Margin="12,0,0,0"
                    Command="{Binding InstallAdkCommand}"
                    Content="{Binding Strings[AdkInstall]}"
                    IsEnabled="{Binding IsOperationInProgress, Converter={StaticResource InverseBooleanConverter}}"
                    Visibility="{Binding IsAdkMissing, Converter={StaticResource BooleanToVisibilityConverter}}" />

                <Button
                    Grid.Column="1"
                    Margin="12,0,0,0"
                    Command="{Binding UpgradeAdkCommand}"
                    Content="{Binding Strings[AdkUpgrade]}"
                    IsEnabled="{Binding IsOperationInProgress, Converter={StaticResource InverseBooleanConverter}}"
                    Visibility="{Binding IsAdkIncompatible, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </Grid>
        </Border>

        <!--  Main Content  -->
        <Grid Grid.Row="2" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel
                Grid.Row="0"
                Margin="0,0,0,12"
                Orientation="Horizontal">
                <Button
                    Width="120"
                    Margin="0,0,8,0"
                    Command="{Binding CreateIsoCommand}"
                    Content="{Binding Strings[CreateIso]}"
                    IsEnabled="{Binding CanCreateMedia}" />

                <Button
                    Width="120"
                    Margin="0,0,8,0"
                    Command="{Binding CreateUsbCommand}"
                    Content="{Binding Strings[CreateUsb]}"
                    IsEnabled="{Binding CanCreateMedia}" />

                <Button
                    Width="120"
                    Command="{Binding RefreshUsbCandidatesCommand}"
                    Content="{Binding Strings[UsbRefreshDisks]}"
                    IsEnabled="{Binding IsRefreshingUsbCandidates, Converter={StaticResource InverseBooleanConverter}}" />
            </StackPanel>

            <Border
                Grid.Row="1"
                Padding="12"
                BorderBrush="{DynamicResource TextFillColorPrimaryBrush}"
                BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,8,4" Text="{Binding Strings[StagingPathLabel]}" />
                    <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" Text="{Binding StagingDirectoryPath, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,8,4" Text="{Binding Strings[IsoOutputPathLabel]}" />
                    <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" Text="{Binding IsoOutputPath, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,8,4" Text="{Binding Strings[ArchitectureLabel]}" />
                    <ComboBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,8" ItemsSource="{Binding AvailableArchitectures}" SelectedItem="{Binding SelectedArchitecture}" />

                    <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,8,4" Text="{Binding Strings[SignatureModeLabel]}" />
                    <ComboBox Grid.Row="3" Grid.Column="1" Margin="0,0,0,8" ItemsSource="{Binding AvailableSignatureModes}" SelectedItem="{Binding SelectedSignatureMode}" />

                    <TextBlock Grid.Row="4" Grid.Column="0" Margin="0,0,8,4" Text="{Binding Strings[VendorLabel]}" />
                    <ComboBox Grid.Row="4" Grid.Column="1" Margin="0,0,0,8" ItemsSource="{Binding AvailableVendors}" SelectedItem="{Binding SelectedVendor}" />

                    <TextBlock Grid.Row="5" Grid.Column="0" Margin="0,0,8,4" Text="{Binding Strings[MediaOptionsLabel]}" />
                    <StackPanel Grid.Row="5" Grid.Column="1" Margin="0,0,0,8">
                        <CheckBox Margin="0,0,0,6" Content="{Binding Strings[IncludeDriversLabel]}" IsChecked="{Binding IncludeDrivers}" />
                        <CheckBox Margin="0,0,0,6" Content="{Binding Strings[IncludePreviewDriversLabel]}" IsChecked="{Binding IncludePreviewDrivers}" />
                    </StackPanel>

                    <TextBlock Grid.Row="6" Grid.Column="0" Margin="0,0,8,4" Text="{Binding Strings[UsbDiskLabel]}" />
                    <ComboBox Grid.Row="6" Grid.Column="1" Margin="0,0,0,8" DisplayMemberPath="DisplayLabel" ItemsSource="{Binding UsbDiskCandidates}" SelectedItem="{Binding SelectedUsbDiskCandidate}" />

                    <TextBlock Grid.Row="7" Grid.Column="0" Margin="0,0,8,4" Text="{Binding Strings[UsbBootDriveLabel]}" />
                    <TextBox Grid.Row="7" Grid.Column="1" Margin="0,0,0,8" Text="{Binding UsbBootDriveLetter, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="8" Grid.Column="0" Margin="0,0,8,4" Text="{Binding Strings[UsbPartitionStyleLabel]}" />
                    <ComboBox Grid.Row="8" Grid.Column="1" Margin="0,0,0,8" ItemsSource="{Binding AvailablePartitionStyles}" SelectedItem="{Binding SelectedPartitionStyle}" />

                    <TextBlock Grid.Row="9" Grid.Column="0" Margin="0,0,8,4" Text="{Binding Strings[UsbExpectedCodeLabel]}" />
                    <TextBlock Grid.Row="9" Grid.Column="1" Margin="0,0,0,8" Text="{Binding ExpectedUsbConfirmationCode}" />

                    <TextBlock Grid.Row="10" Grid.Column="0" Margin="0,0,8,4" Text="{Binding Strings[UsbConfirmationCodeLabel]}" />
                    <TextBox Grid.Row="10" Grid.Column="1" Margin="0,0,0,8" Text="{Binding UsbConfirmationCode, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="11" Grid.Column="0" Margin="0,0,8,4" Text="{Binding Strings[UsbConfirmationCodeRepeatLabel]}" />
                    <TextBox Grid.Row="11" Grid.Column="1" Text="{Binding UsbConfirmationCodeRepeat, UpdateSourceTrigger=PropertyChanged}" />
                </Grid>
            </Border>
        </Grid>

        <!--  Global Operation Status  -->
        <Border
            Grid.Row="3"
            Padding="12,8"
            Background="Transparent"
            BorderBrush="{DynamicResource TextFillColorPrimaryBrush}"
            BorderThickness="0,1,0,1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock
                    Grid.Row="0"
                    Margin="0,0,0,6"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Text="{Binding GlobalOperationStatusDisplay}" />

                <ProgressBar
                    Grid.Row="1"
                    Height="14"
                    Maximum="100"
                    Minimum="0"
                    Value="{Binding GlobalOperationProgress, Mode=OneWay}" />

                <TextBlock
                    Grid.Row="2"
                    Margin="0,6,0,0"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Text="{Binding MediaActionMessage}" />
            </Grid>
        </Border>

        <!--  Footer  -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border
                Grid.Column="0"
                Padding="12,8"
                BorderBrush="{DynamicResource TextFillColorPrimaryBrush}"
                BorderThickness="0,0,1,0">
                <TextBlock
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Text="{Binding UsbDiskCandidates.Count, StringFormat=USB devices: {0}}" />
            </Border>

            <Border Grid.Column="1" Padding="12,8">
                <TextBlock
                    HorizontalAlignment="Right"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Text="Version: 1.0.0.0" />
            </Border>
        </Grid>
    </Grid>
</Window>
