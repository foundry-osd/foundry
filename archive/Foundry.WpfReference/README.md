# Foundry WPF Reference Archive

This folder keeps the former WPF Foundry application as a read-only migration reference.

The archived project file is intentionally named `Foundry.csproj.reference` so repository-wide tooling does not restore, build, or submit dependencies for obsolete WPF reference code.
